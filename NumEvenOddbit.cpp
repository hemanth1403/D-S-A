#include <bits/stdc++.h>
using namespace std;

int main(){
    int a;
    cin>>a;
    if((a & 1) == 0) cout<<"Number is Even"; // While using any bitwise operator enclose them in a () brackets
    else cout<<"Number is Odd";
    // cout<<(a & 1);
}